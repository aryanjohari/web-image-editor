# Server only — set in Vercel project env or `vercel dev` (never use VITE_ prefix)
OPENAI_API_KEY=sk-...

# Optional server override (defaults to gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Client toggle — "true" attempts POST /api/mood before keyword fallback
VITE_MOOD_AI_ENABLED=true
