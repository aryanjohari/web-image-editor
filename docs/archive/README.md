# Archived architecture IR

These files were the old **portfolio flowchart IR** (Mermaid + `architecture.graph.json` tour schema).

They are **not** the source of truth anymore. Canonical diagrams live under [`docs/c4/`](../c4/).

Kept only so historical portfolio/build references can be compared; do not regenerate or extend them.
